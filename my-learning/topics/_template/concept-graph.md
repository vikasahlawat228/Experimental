# Concept graph

How concepts in this topic connect. Used by `daily-learning` to drive the "callback" move — bridging today's new concept to a prior one I already own.

> As of YYYY-MM-DD.

## Concepts so far

- _(populated as modules introduce concepts)_

## Connections

Each connection: from → to, with a one-line "bridge sentence" the assistant can use.

| From | To | Bridge sentence |
|---|---|---|
| _(populated when a connection lands during a session)_ |  |  |

## Optional: Mermaid view

If the graph gets dense, render a mermaid diagram here for at-a-glance. Otherwise, leave the table as the source of truth.

```mermaid
graph LR
  %% populated over time
```
